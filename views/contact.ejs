<section class="page__container"></section>
<h1 class="page-title"><%- title %></h1>
      <div class="form-container">
      <h2 class="form-title">Agenda tu cita aquí 📩</h2>
      <p class="header-paragraph">👋, si te interesa conversar, aquí estaré, deja tus datos en el siguiente formulario y te escribiré de vuelta.</p>
        <form class="contact-form" action="/contacto" method="POST">
            <div class="form-group">
                <label class="form-label" for="name">Nombre</label>
                <input class="form-input" id="name" name="name" type="text" placeholder="Escribe tu nombre completo" required <% if (errors && errors.name) { %>
                  class="form__input--error"
                <% } %>>
                <% if (errors && errors.name) { %>
                  <p class="form__error"><%= errors.name.msg %></p>
                <% } %>
            </div>
            <div class="form-group">
              <label class="form-label" for="phone">Teléfono</label>
              <input class="form-input" id="phone" type="phone" name="phone" placeholder="Digita un número de teléfono"
                  <% if (errors && errors.phone) { %>
                    class="form__input--error"
                  <% } %>>
                <% if (errors && errors.phone) { %>
                  <p class="form__error"><%= errors.phone.msg %></p>
                <% } %>
          </div>
            <div class="form-group">
                <label class="form-label" for="email">Email</label>
                <input class="form-input" id="email" name="email" type="email" placeholder="Escribe tu Email" required <% if (errors && errors.email) { %>
                  class="form__input--error"
                <% } %>>
                <% if (errors && errors.email) { %>
                  <p class="form__error"><%= errors.email.msg %></p>
                <% } %>
            </div>
            <div class="form-group">
              <div class="help">Por favor selecciona el tipo de servicio en el que te encuentras interesado.</div>
                <label for="type">Servicios</label>
                <select id="type" name="type" required>
                    <option value="" disabled selected>Selecciona uno</option>
                    <option value="terapia-individual">Terapia individual</option>
                    <option value="terapia-de-pareja">Terapia de pareja</option>
                    <option value="terapia-familiar">Terapia familiar</option>
                    <option value="terapia-de-grupo">Terapia de grupo</option>
                    <option value="terapia-institucional">Terapia institucional</option>
                    <option value="clase-o-conferencia">Clase magistral o conferencia</option>
                </select>
            </div>
            <div class="form-group">
                <label for="description">Mensaje</label>
                <textarea class="form-textarea" name="description" id="description" cols="40" rows="10" placeholder="Háblame sobre ti y describe brevemente el tema del cuál quieres hablar"></textarea>
                <div class="help">El mensaje no puede tener más de 500 palabras.</div>
            </div>
            <div class="form-group">
                <label for="terms">
                    <input id="terms" type="checkbox" name="terms" required>
                    <span>Acepto términos y condiciones</span>
                </label>
            </div>
            <div class="button-container">
                <input type="submit" value="Enviar" class="send-button">
            </div>
            <% if (typeof success_msg !== 'undefined' && success_msg) { %>
              <div class="success-msg">
                <p>
                  <%= success_msg %>
                </p>
              </div>
              <% } else { %>
                <div class="help">
                  <p>Haga click en el botón para enviar el formulario</p>
                </div>
                <% } %>
        </form>

</section>
